# GRPO Algorithm Configuration
defaults: "grpo_infinisst.yaml"

grpo:
  num_generations_per_prompt: 16
  reduce_baseline_rewards: false
  normalize_rewards: false

policy:
  train_global_batch_size: 512

  generation:
    top_p: 0.999
    top_k: 10000
    max_new_tokens: 40
    vllm_kwargs:
      enable_prompt_embeds: false
  
  optimizer:
    name: "torch.optim.AdamW"
    kwargs:
      lr: 5e-7
      weight_decay: 0.01
      betas: [0.9, 0.999]
      eps: 1e-8
      # when using Dtensor, we need to set foreach
      # and fused to False
      foreach: False
      fused: False

  scheduler:
    - name: "torch.optim.lr_scheduler.ConstantLR"
      kwargs:
        factor: 1.0
        total_iters: 10000000000
    - milestones: []

env:
  infinisst:
    cfg:
      scoring_model_type: "google/metricx-24-hybrid-xxl-v2p6-bfloat16"
      scoring_model_path: "/ckpts/llm/metricx-24-hybrid-xxl-v2p6-bfloat16"
      scoring_tokenizer_path: "/ckpts/llm/mt5-xl"
      qe: false
      alpha: 1.0
      beta: 0.0
      max_latency: 10.0
      normalize: true
  
checkpointing:
  checkpoint_dir: "/ckpts/infinisst-rl/grpo-metricx-laal-explore-p0.999-fast"

logger:
  wandb:
    project: "infinisst-rl"
    name: "grpo-metricx-laal-explore-p0.999-fast"